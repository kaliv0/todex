TODO extractor

read flags (auto-validated by lib)
resolve path if existing - if not passed use cwd
read files:
  for f in files:
    read
    find TODO
    if line_count:  # e.g. TODO{3} ...
      read next n lines
    append result to TODO.md file
    
if --recursive -> step into nested dirs and repeat
if --max-depth -> stop when necessary

if --out -> save to "out" file
else -> save to TODO.md
if file exists -> override
  unless --append passed
  
save TODO.md in path/cwd
(what of --out is full_path, not just file name??)

TODO{5}
by-default search for TODO, FIXME
user can pass custom (varargs) -> WARN, REVISIT

if not line_count -> save only line with TODO (full line or just TODO comment?)
optionally - pass flag to skips code lines/snippets -> save only TODO comments

each pargraph starts with path:line_number e.g. ./foo/bar/fizz.rs:35
(full path or relative? from cwd)
+ TODO comment + optional snippet (+ optional []  to conveniently add [x] or [DONE] later)

--follow-symlinks -> by default false

add colors inside outfile e.g. red/blue for paths??

While reading file -> read buffered line by line, keep curr_line counter (needed for output)

What if we can optionally just go through files and log analysis in terminal without saving it
- or both/ tee shell+save ??

Flags:
- out [FILE_PATH]
- recursive
- max-depth [n]
- follow-symlinks
- tokens (e.g. WARN, DEFER, REVISIT) (decide on name?!)
- exclude/skip [glob for dirs/files]
- no-snippets
- full-path (for each paragraph)
